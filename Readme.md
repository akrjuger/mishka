Проект для обучения адаптивной верстки. Макет взят из открытых источников.

Для старта проекта нужно запустить:
<code>npm i</code>
<code>npm start</code>

Стартует Browser-Sync и отсдеживает изменения в папке source
